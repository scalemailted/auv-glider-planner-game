# DIVE-R1.1 Visual Acceptance

The full headed screenshot package is not implemented in this pass. Use this checklist when creating or reviewing it.

Expected artifacts should cover segment profile editing, mixed predicted profiles, active layer values, stacked slabs, integrated water column, vertical profile, layer difference, live profile transition, actual-depth observation, surfacing replan profile edits, replay segment profile review, main-menu cleanup, and compact layout.

Required acceptance statement: Human manual QA by the project owner remains pending until the DIVE-R1.1 segment-profile and water-column layer screenshot package is reviewed.

Performance acceptance should report average, p50, p95, p99, max frame interval, rendered FPS, renderer count, RAF count, render submissions, textured slab count, context slab count, and cleanup counts.
