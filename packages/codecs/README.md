# @anchor/codecs

Owns future JSON/CSV/bundle codec boundaries and visibility-safe artifact serialization contracts.

ARCH-R1 is a skeleton only. Existing exporters, solver-packet adapters, and headless bundle codecs remain under `src/core/` until targeted extraction phases.

Allowed dependency: `@anchor/contracts`.
