# @anchor/environment

Owns the future composition layer that combines bathymetry, currents, and scalar processes into synthetic environment manifests and artifacts.

ARCH-R1 is a skeleton only. Existing environment assembly modules remain under `src/core/environment/`.

Allowed dependencies: `@anchor/contracts`, `@anchor/bathymetry`, `@anchor/currents`, and `@anchor/scalar-processes`.
