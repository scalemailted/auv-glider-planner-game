# @anchor/bathymetry

Owns the future bathymetry, terrain, land/sea mask, slope, and depth-accessibility scientific package boundary.

ARCH-R1 contains a small contract proof that wraps `@anchor/contracts` bathymetry manifest and artifact helpers. It does not move or rewrite existing bathymetry algorithms.

Allowed dependency: `@anchor/contracts`.
