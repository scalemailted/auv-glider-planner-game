# @anchor/validation

Owns future package-level scientific validation suites, fixture checks, artifact compatibility checks, and model-claim boundary checks.

ARCH-R1 is a skeleton only. Existing repository smoke and E2E tools remain in `tools/js/` and `tests/`.
